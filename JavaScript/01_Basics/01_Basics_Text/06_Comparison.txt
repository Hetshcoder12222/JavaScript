1.:

The reason is that an equality check == and comparisons > < >= <= work differently.
Comparisons convert null to a number, treating it as 0.
That's why (3) nuil >= 0 is true and (1) null > 0 is false.



Difference between === and == in javascript:

In JavaScript, == and === are comparison operators, but they behave differently:


✅ == (Loose Equality)                                                                         | ✅ === (Strict Equality)
                                                                                                |
Compares values, but ignores the data type.                                                     |Compares both value and data type.
No type conversion is done.                                                                     |Performs type coercion if needed (tries to convert one type to another before comparing).

🔑 Summary:

Operator	  |      Compares	    |    Type Conversion?	  |      Safer to Use?
              |                     |                         |
==	          |      Value only	    |     Yes	              |      ❌ Risky
===	          |      Value + Type	|     No	              |      ✅ Recommended